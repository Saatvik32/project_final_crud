<%- include("partials/header"); %>

<h1>Home</h1>
<p><%=homecontent %></p>
<% posts.forEach(function(item){ %>
    <div class="item">
        <h1><%= item.title %></h1>
        <p>
        <%= item.content.substring(0,100) + "..." %> <a href="/posts/<%=item._id%>"> Read more</a>
    </p>
    </div>
<% }) %>
<%- include("partials/footer"); %>